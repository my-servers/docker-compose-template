services:
  adguardhome:
    image: adguard/adguardhome:latest
    container_name: {{container_name}}
    restart: unless-stopped
    network_mode: host
    volumes:
{{#volumes}}
      - {{source}}:{{target}}:{{mode}}
{{/volumes}}
