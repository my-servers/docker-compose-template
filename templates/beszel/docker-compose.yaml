services:
  beszel:
    image: henrygd/beszel:latest
    container_name: {{container_name}}
    restart: unless-stopped
    ports:
      - "{{http_port}}:8090"
    volumes:
      - {{data_dir}}:/beszel_data
    environment:
      - TZ=Asia/Shanghai
