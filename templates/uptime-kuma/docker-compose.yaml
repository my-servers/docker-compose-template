services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    container_name: {{container_name}}
    restart: unless-stopped
    ports:
      - "{{http_port}}:3001"
    volumes:
      - {{data_dir}}:/app/data
    environment:
      - PUID={{puid}}
      - PGID={{pgid}}