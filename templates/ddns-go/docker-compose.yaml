services:
  ddns-go:
    image: jeessy/ddns-go:latest
    container_name: {{container_name}}
    restart: always
    network_mode: host
    volumes:
{{#volumes}}
      - {{source}}:{{target}}:{{mode}}
{{/volumes}}
